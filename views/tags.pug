extends layout.pug
block header
	title #{blog.name}-所有标签
	link(href="../css/style.css",rel="stylesheet")
block content
	.container.tagItems
		each item in tags
			a(href=`/tag/${item}`) #{item}
		else
			| 还没有标签